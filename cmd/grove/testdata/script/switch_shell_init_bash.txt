# Test: grove switch shell-init --shell bash generates POSIX function
setup_workspace

exec grove switch shell-init --shell bash
stdout 'grove\(\)'
stdout 'GROVE_SHELL=1 command grove'
stdout 'case "\$\{1-\}" in'
! stdout '\[\['
stdout 'GROVE_PREV_WORKTREE'
stdout 'no previous worktree'
stdout 'command grove remove'
! stderr .
