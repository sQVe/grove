# Test: grove list shows dirty indicator for untracked files
setup_workspace

cp $WORK/untracked.txt untracked.txt
exec grove list --plain
stdout '\[dirty\]'
stderr 'Gathering worktree status'

-- untracked.txt --
new
