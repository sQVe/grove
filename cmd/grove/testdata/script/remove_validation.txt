# grove remove validation tests
# Tests remove command argument parsing and pre-condition validation

## Help output

exec grove remove --help
stdout 'Remove one or more worktrees'
stdout 'Usage:'
stdout '<worktree>...'
stdout '--branch'
stdout '--force'
! stderr .

## Argument validation

# No arguments
! exec grove remove
stderr 'at least 1 arg'

## Workspace validation

# Not in workspace
cd /tmp
! exec grove remove feature
stderr 'not in a grove workspace'
