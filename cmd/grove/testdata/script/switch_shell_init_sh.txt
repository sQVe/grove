# Test: grove switch shell-init --shell sh generates POSIX function
setup_workspace

exec grove switch shell-init --shell sh
stdout 'grove\(\)'
stdout 'GROVE_SHELL=1 command grove'
stdout 'command grove switch'
stdout 'case "\$1" in'
stdout 'grove add'
stdout '\-\-switch'
! stdout '\[\['
stdout 'GROVE_PREV_WORKTREE'
stdout 'no previous worktree'
stdout 'command grove remove'
! stderr .
