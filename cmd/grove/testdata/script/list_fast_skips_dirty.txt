# Test: grove list --fast skips dirty status
setup_workspace

cp $WORK/dirty.txt dirty.txt
exec grove list --fast --plain
! stdout '\[dirty\]'
stderr 'Gathering worktree status'

-- dirty.txt --
dirty
