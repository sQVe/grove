version: '2'

run:
  timeout: 5m
  modules-download-mode: readonly

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    gofumpt:
      module-path: 'github.com/sqve/grove'
      extra-rules: true

linters:
  exclusions:
    rules:
      - path: '(.+)_test\.go'
        linters:
          - goconst
      # SA5011 false positives: t.Fatal terminates the test, but staticcheck's
      # flow analysis doesn't know that. Scope narrowly to files where the
      # "if x == nil { t.Fatal }; x.Field" pattern is used.
      - path: 'cmd/grove/commands/(clone|exec|fetch|list|lock|prune|remove|status)_test\.go'
        text: 'SA5011'
        linters:
          - staticcheck
      - path: 'internal/git/worktree_test\.go'
        text: 'SA5011'
        linters:
          - staticcheck
  enable:
    # Essential Go checks
    - errcheck # Check for unchecked errors
    - govet # Vet examines Go source code
    - ineffassign # Detect ineffective assignments
    - staticcheck # Static analysis checks
    - unused # Check for unused code

    # Bug prevention
    - bodyclose # Check HTTP response body is closed
    - copyloopvar # Check loop variable copying (Go 1.22+)

    # Code quality
    - goconst # Find repeated strings for constants
    - gocritic # Comprehensive diagnostics

    # Comprehensive unused code detection
    - unparam # Unused function parameters
    - unconvert # Unnecessary type conversions
    - wastedassign # Wasted assignments

    # Security and error handling
    - gosec # Security vulnerabilities
    - errorlint # Error wrapping and handling issues
    - nilerr # nil error return issues
    - nilnil # Check for simultaneous nil return
    - contextcheck # Context usage validation
    - testifylint # Testify best practices

  settings:
    # Configure unused linter for comprehensive detection
    unused:
      exported-fields-are-used: false
      local-variables-are-used: true
      parameters-are-used: true

    # Configure unparam for unused parameters
    unparam:
      check-exported: true # Check exported functions for unused params

    # Configure goconst to ignore common boolean and numeric strings
    goconst:
      min-len: 3
      min-occurrences: 3
      ignore-string-values:
        - '^(true|false|yes|no|on|off)$' # Boolean strings
        - '^[0-9]+$' # Numeric strings like "1", "0"
        - '^origin$'

    gocritic:
      enabled-tags:
        - diagnostic
        - performance
        - style
      disabled-checks:
        - dupImport
        - whyNoLint
