kind: Changed
body: 'Internal worktree creation now uses one Git helper, one branch loop, and one auto-lock path. Dead types and duplicate helpers were removed, and status now reuses the shared worktree record.'
